spring.datasource.url: jdbc:h2:file:./.data/h2/camunda-kafka-demo-db

camunda.bpm:
  admin-user:
    id: demo
    password: demo
  filter.create: All Tasks

kafka:
  bootstrap-address: "${KAFKA_URI:localhost}"
  group-id: someGroupId
  trusted-packages: org.camunda.bpm.demo.dto